<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="main.css">
    <link rel="stylesheet" type="text/css" href="hero-card.css">
    <title>Dota tbh</title>
</head>
<body>
    <div class="grid-container">
        <div class="item1">
            <h1>Dota Hero</h1>
        </div>
        <div class="item2">
            <h1>
                <%= Match.match_id %>
            </h1>         
        </div>
        <div>
            <% if (typeof Match !== 'undefined' && Match.players) { %>
                <% for(let i = 0; i<5; i++){ %>
                    <%- include("hero-card.ejs", { Player: Match.players[i] }) %>
                <% }; %>
            <% } else { %>
                <p>Something went wrong. No match data found.</p>
            <% } %> 
        </div>
        <div>
            <% if (typeof Match !== 'undefined' && Match.players) { %>
                <% for(let i = 5; i<10; i++){ %>
                    <%- include("hero-card.ejs", { Player: Match.players[i] }) %>
                <% }; %>
            <% } else { %>
                <p>Something went wrong. No match data found.</p>
            <% } %> 
        </div>
    </div>
    
</body>
</html>